# Minimal Node.js devcontainer Dockerfile
FROM mcr.microsoft.com/devcontainers/node:20-bookworm

# Use the default non-root user provided by the image
USER vscode

WORKDIR /workspaces/sigma-gantt-plugin

CMD ["sleep", "infinity"]
